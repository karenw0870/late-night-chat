import { browser, logging, protractor, Ptor } from 'protractor';
import { GroupsPage } from './groups.po';

describe('>> Group Page', () => {
  let page: GroupsPage;

  beforeEach(() => {
    page = new GroupsPage();
  });

});
