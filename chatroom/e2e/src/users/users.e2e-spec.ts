import { UsersPage } from './users.po';
import { browser, logging, protractor, Ptor } from 'protractor';

describe('>> Login Page', () => {
  let page: UsersPage;

  beforeEach(() => {
    page = new UsersPage();
  });

});
